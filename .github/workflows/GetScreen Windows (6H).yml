name: GetScreen Windows (6H)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=fegipi7924@dixiser.com" > secrets.txt
         Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/8iina9e4f0adarlb2umjv/down.bat?rlkey=vrpbfdyr3vhh7ymtfxux5gwoh&dl=0" -OutFile "down.bat"
         cmd /c down.bat

      - name: Time Counter
        run: python time.py
